let myVariable = "Hello world!";
console.log(myVariable);
myVariable = "Hello universe!";
console.log(myVariable);

let name = "Lambert";
let firstName = "Gerard";
let city = "Paris";

console.log("Your name is " + firstName + " " + name + " and you live in " + city + ".");
console.log(`Your name is ${firstName} ${name} and you live in ${city}.`);

let a = 5;
let b = 10;
let sum = a + b;
console.log("The sum of " + a + " and " + b + " is " + sum + ".");
console.log(`The sum of ${a} and ${b} is ${sum}.`);

let age = 25;
let height = 175; // in cm
let weight = 70; // in kg

console.log(`You are ${age} years old, ${height} cm tall and weigh ${weight} kg.`);

let x = 15;
let y = 30;
let product = x * y;
console.log("The product of " + x + " and " + y + " is " + product + ".");
console.log(`The product of ${x} and ${y} is ${product}.`);

let country = "France";
let language = "French";
let population = 67000000;

console.log(`You live in ${country}, where the official language is ${language} and the population is approximately ${population} people.`);

let length = 8;
let width = 5;
let area = length * width;
console.log("The area of a rectangle with length " + length + " and width " + width + " is " + area + ".");
console.log(`The area of a rectangle with length ${length} and width ${width} is ${area}.`);

let temperature = 22; // in Celsius
let weather = "sunny";

console.log(`The current temperature is ${temperature}°C and the weather is ${weather}.`);

let radius = 7;
let circumference = 2 * Math.PI * radius;
console.log("The circumference of a circle with radius " + radius + " is " + circumference.toFixed(2) + ".");
console.log(`The circumference of a circle with radius ${radius} is ${circumference.toFixed(2)}.`);

let bookTitle = "JavaScript Basics";
let author = "John Doe";
let pages = 350;

console.log(`The book "${bookTitle}" by ${author} has ${pages} pages.`);

let speed = 60;     // in km/h
let time = 2;       // in hours
let distance = speed * time;
console.log("The distance traveled at a speed of " + speed + " km/h for " + time + " hours is " + distance + " km.");
console.log(`The distance traveled at a speed of ${speed} km/h for ${time} hours is ${distance} km.`);

let movieName = "Inception";
let director = "Christopher Nolan";
let releaseYear = 2010;

console.log(`The movie "${movieName}" directed by ${director} was released in ${releaseYear}.`);

let base = 4;
let heightTriangle = 6;
let areaTriangle = 0.5 * base * heightTriangle;
console.log("The area of a triangle with base " + base + " and height " + heightTriangle + " is " + areaTriangle + ".");
console.log(`The area of a triangle with base ${base} and height ${heightTriangle} is ${areaTriangle}.`);

let company = "TechCorp";
let industry = "technology";
let employees = 5000;

console.log(`The company ${company} operates in the ${industry} industry and has approximately ${employees} employees.`);

let fahrenheit = 68;
let celsius = (fahrenheit - 32) * 5 / 9;
console.log(fahrenheit + "°F is equal to " + celsius.toFixed(2) + "°C.");
console.log(`${fahrenheit}°F is equal to ${celsius.toFixed(2)}°C.`);

let laptopBrand = "Dell";
let model = "XPS 13";
let ram = 16; // in GB

console.log(`The laptop is a ${laptopBrand} ${model} with ${ram}GB of RAM.`);

let principal = 1000;
let rate = 5; // annual interest rate in %
let timeYears = 3;
let interest = (principal * rate * timeYears) / 100;
console.log("The interest on a principal of " + principal + " at a rate of " + rate + "% for " + timeYears + " years is " + interest + ".");
console.log(`The interest on a principal of ${principal} at a rate of ${rate}% for ${timeYears} years is ${interest}.`);

let smartphoneBrand = "Apple";
let modelPhone = "iPhone 13";
let storage = 128; // in GB

console.log(`The smartphone is an ${smartphoneBrand} ${modelPhone} with ${storage}GB of storage.`);

let sides = 6;
let perimeter = sides * 4; // assuming a square
console.log("The perimeter of a square with side length " + sides + " is " + perimeter + ".");
console.log(`The perimeter of a square with side length ${sides} is ${perimeter}.`);

let albumTitle = "Greatest Hits";
let artist = "Famous Band";
let tracks = 12;

console.log(`The album "${albumTitle}" by ${artist} contains ${tracks} tracks.`);

let fuelEfficiency = 15; // in km/l
let fuelConsumed = 20;   // in liters
let distanceTraveled = fuelEfficiency * fuelConsumed;
console.log("The distance traveled with a fuel efficiency of " + fuelEfficiency + " km/l and " + fuelConsumed + " liters of fuel is " + distanceTraveled + " km.");
console.log(`The distance traveled with a fuel efficiency of ${fuelEfficiency} km/l and ${fuelConsumed} liters of fuel is ${distanceTraveled} km.`);

let tvBrand = "Samsung";
let screenSize = 55; // in inches
let resolution = "4K";

console.log(`The TV is a ${tvBrand} with a screen size of ${screenSize} inches and ${resolution} resolution.`);

let principalAmount = 2000;
let annualRate = 3;